{
  "id": "scenario-3-os-time",
  "goal": "Force a real trust-posture transition: solve-pure -> os-time. TrustCard must change color, rung gates must lose 'Solve-world default'.",
  "intent_override": "Build a timer that measures and reports elapsed wall-clock time between two events.",
  "expected_flow": [
    "Agent imports std.os.time during impl",
    "Real x07 xtal verify --allow-os-world runs",
    "Trust posture captures solve-pure baseline plus run-os world and os-time capability",
    "Posture color flips green -> amber",
    "Shareable rung loses solve-world default; needs trusted_program_sandboxed_* profile",
    "Compare lens (semantic diff) headline mentions world widen"
  ],
  "success_criteria": [
    "TrustCard shows run-os world chip, os-time capability chip, and amber border color",
    "Semantic diff headline references world widen",
    "Climb to Shareable fails until trust profile is added"
  ]
}
